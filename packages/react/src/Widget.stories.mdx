import { Canvas, Meta, Story } from "@storybook/addon-docs";

import Widget from "./Widget";

<Meta title="Widget" component={Widget} />

# Widget

export const Template = (args) => <Widget {...args} />;

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
